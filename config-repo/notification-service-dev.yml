server:
  port: 8083

spring:
  kafka:
    enabled: false
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

logging:
  level:
    com.ticketdesk: DEBUG
